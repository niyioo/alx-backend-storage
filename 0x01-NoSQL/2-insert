#!/bin/bash
# Script to insert a document in the collection school

# Check if the database name is provided
if [ -z "$1" ]; then
    echo "Usage: $0 <database_name>"
    exit 1
fi

# Connect to MongoDB and insert the document
mongo "$1" --quiet --eval 'db.school.insert({ name: "Holberton school" });'
